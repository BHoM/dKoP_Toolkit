{
  "$id" : "https://raw.githubusercontent.com/BHoM/dKoP_Toolkit/TestSchema/.JsonSchema/Geometry/IPolyCurve.json",
  "if" : {
    "required" : ["_t"],
    "description" : "If a type discirminator (_t) has been set, use that to find the type to validate against."
  },
  "then" : {
    "description" : "All of with If-then acts as a switch-case based on the value of _t. Switched based on the type defined.",
    "allOf" : [{
        "if" : {
          "properties" : {
            "_t" : {
              "const" : "BH.oM.Geometry.PolyCurve"
            }
          }
        },
        "then" : {
          "$ref" : "https://raw.githubusercontent.com/BHoM/dKoP_Toolkit/TestSchema/.JsonSchema/Geometry/PolyCurve.json"
        }
      }, {
        "if" : {
          "properties" : {
            "_t" : {
              "const" : "BH.oM.Geometry.BoundaryCurve"
            }
          }
        },
        "then" : {
          "$ref" : "https://raw.githubusercontent.com/BHoM/dKoP_Toolkit/TestSchema/.JsonSchema/Geometry/BoundaryCurve.json"
        }
      }]
  },
  "else" : {
    "description" : "For case of no type-discriminator defined, the data is matched based on a oneOff pattern. This can for edgecases give errors if two classes mapped in here share the exact same properties.",
    "oneOf" : [{
        "$ref" : "https://raw.githubusercontent.com/BHoM/dKoP_Toolkit/TestSchema/.JsonSchema/Geometry/PolyCurve.json"
      }, {
        "$ref" : "https://raw.githubusercontent.com/BHoM/dKoP_Toolkit/TestSchema/.JsonSchema/Geometry/BoundaryCurve.json"
      }]
  }
}